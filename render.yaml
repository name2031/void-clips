services:
  - type: web
    name: void-ai
    runtime: node
    buildCommand: npm ci && npm run build
    startCommand: node server/index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: OWNER_EMAIL
        value: yuel.zeru2000@gmail.com
